import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "FGC Expertise | Consultoria Empresarial 360º",
  description: "Transforme a gestão do seu negócio com a metodologia exclusiva FGC. Consultoria, Mentoria e Diagnóstico 360º.",
  icons: {
    icon: '/logo-fgc.png', // Aqui ele pega a sua logo para usar de ícone
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
